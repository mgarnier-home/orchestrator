name: backup

services:
  # go-autosaver:
  #   container_name: go-autosaver
  #   restart: unless-stopped
  #   image: mgarnier11/go-autosaver:${GO_AUTOSAVER_VERSION}
  #   volumes:
  #     - docker-data-local:/backups
  #     - type: volume
  #       source: docker-data
  #       volume:
  #         subpath: autosaver
  #       target: /config
  #   ports:
  #     - ${APOLLON_AUTOSAVER_PORT}:8080
  #   labels:
  #     traefik-conf.port: ${APOLLON_AUTOSAVER_PORT}
  #     traefik-conf.name: autosaver.apollon
  #   privileged: true
  #   environment:
  #     CONFIG_FILE_PATH: /config/config-apollon.yml
  #     # LOG_LEVEL: 5
  #     NTFY_SERVER: ${NTFY_SERVER}
  #     NTFY_TOPIC: ${NTFY_TOPIC}
  #     SSH_PRIVATE_KEY: ${SSH_PRIVATE_KEY}

volumes:
  docker-data-local:
    driver: local
    driver_opts:
      type: none
      device: ${APOLLON_DOCKER_DATA}
      o: bind
  docker-data:
    external: true
